/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.tools.unlayer.com",
      "fonts.gstatic.com",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
